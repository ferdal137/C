#include <stdio.h>

int main() {
    // Declaración e inicialización de un arreglo de caracteres
    char saludo[] = "Hola";

    // Imprimir la cadena
    printf("Saludo: %s\n", saludo);

    return 0;
}
